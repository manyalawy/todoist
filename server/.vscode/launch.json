{
    "configurations": [
        {
            "type": "java",
            "name": "Spring Boot-ServerApplication<board>",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "mainClass": "com.todoist.server.ServerApplication",
            "projectName": "board",
            "args": "",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}